{
  "name": "macdisk-agent",
  "version": "1.0.0",
  "description": "Local WebSocket agent for MacDisk Analyzer — runs on your Mac, analyzes disk usage",
  "main": "server.js",
  "bin": {
    "macdisk-agent": "./server.js"
  },
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "ws": "8.18.0"
  },
  "engines": {
    "node": ">=18",
    "os": ["darwin"]
  },
  "keywords": ["macos", "disk", "storage", "analyzer", "cleaner"],
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/IsaacMendoza-dv/macbook-disk-analyzer"
  },
  "files": ["server.js", "lib/"],
  "notes": {
    "audit": "ws@8.18.0 is the latest available version. The high severity advisory (CVE-2026-45736) has no patched release yet. The agent runs on localhost only and is not exposed to the network, so the attack surface is minimal."
  }
}
